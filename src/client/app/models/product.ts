export class Product {
  id: number;
  name: string;
  images: Array < any >;
  oldPrice: number;
  newPrice: number;
  discount: number;
  ratingsCount: number;
  ratingsValue: number;
  description: string;
  availibilityCount: number;
  color: Array < string >;
  size: Array < string >;
  weight: number;
  categoryId: number;
}
